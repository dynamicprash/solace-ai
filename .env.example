# Copy to .env and adjust (never commit .env)

# PostgreSQL — async URL is derived automatically (asyncpg / psycopg)
DATABASE_URL=postgresql://postgres:man32109@localhost:5432/solace_ai_db

SECRET_KEY=change-this-to-a-long-random-string
GROQ_API_KEY=
